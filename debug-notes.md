# Debug Notes

## API Status
- All tRPC calls working: auth.me, votes.counts, votes.myVotes, comments.counts
- User is authenticated as Joakim Jardenberg (admin)
- No console errors

## Issue
- Vote and comment buttons not found via keyword search
- The page is in EN mode (language switcher was toggled previously)
- Need to scroll to a specific policy card to see the buttons
- The buttons appear AFTER the "READ MORE" and "SHARE THIS POINT" text in each card
- The screenshot showed the page scrolled to a blank area between sections
